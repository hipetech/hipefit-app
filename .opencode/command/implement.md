---
description: Execute an existing plan using the project's implement skill.
agent: build
---

Load the `implement` skill and follow it in full.

Treat `$ARGUMENTS` as additional user instructions about which initiative in `docs/plans/` to
execute. Do not skip the skill's refusal to run without a plan, the resume-from-status check, the
one-agent-per-task dispatch, the wave-boundary gate, or the abort on a blocked task. Never commit.
