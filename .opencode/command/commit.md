---
description: Commit changes using the project's commit skill.
agent: build
---

Load the `commit` skill and follow it in full.

Treat `$ARGUMENTS` as additional user instructions about commit scope or organization. Do not skip
the skill's branch safety check, explicit scope confirmation, type-check gate, staged diff review,
or commit hooks.
