---
description: Open a pull request using the project's pull-request skill.
agent: build
---

Load the `pull-request` skill and follow it in full.

Treat `$ARGUMENTS` as additional user instructions for the pull request. Do not skip the skill's
required source and target branch confirmation, readiness checks, diff review, or validation gates.
