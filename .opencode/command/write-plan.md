---
description: Write an implementation plan using the project's write-plan skill.
agent: plan-creator
---

Load the `write-plan` skill and follow it in full.

Treat `$ARGUMENTS` as additional user instructions about what to plan. Do not skip the skill's
substantial-work threshold check, read-only research, confirmation of the task split, or the
disjoint `owns:` rule for tasks in the same wave.
