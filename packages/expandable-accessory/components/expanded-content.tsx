import type { ExpandedContentProps } from '../types';

/** Declarative slot for the full-screen expanded destination. */
export const ExpandedContent: React.FC<ExpandedContentProps> = ({ children }) =>
  children;
