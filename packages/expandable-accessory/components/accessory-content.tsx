import type { AccessoryContentProps } from '../types';

/** Declarative slot for the compact native-tab accessory content. */
export const AccessoryContent: React.FC<AccessoryContentProps> = ({
  children,
}) => children;
